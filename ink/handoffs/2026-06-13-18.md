---
date: 2026-06-13
session: foundation
---

# Handoff — 2026-06-13

## ทำอะไรไปแล้ว (Step 1–6 ✅)

| Step | ไฟล์                                                     | สถานะ |
| ---- | -------------------------------------------------------- | ----- |
| 1    | `package.json`, `tsconfig.json`, `drizzle.config.ts`     | ✅    |
| 2    | `src/db/schema.ts` — 10 tables                           | ✅    |
| 3    | `src/db/index.ts` — Bun SQLite singleton + WAL           | ✅    |
| 3    | `src/db/migrate.ts` — FTS5 virtual table + sync triggers | ✅    |
| 4    | `src/bootstrap.ts` — auto-setup DB + dirs + FTS5         | ✅    |
| —    | `bun install` + smoke test bootstrap                     | ✅    |

**DB verified:** soma_users, soma_documents, soma_edges, soma_threads, soma_messages, soma_traces, soma_token_usage, soma_tasks, soma_rate_limits, soma_security_events + soma_fts (FTS5)

## ยังค้างอยู่

- **Step 7:** `src/auth/tokens.ts` + `src/auth/middleware.ts` (bcrypt, Bearer → userId, prefix lookup, lock)
- **Step 8:** `src/mcp.ts` — MCP stdio server wiring + SOMA_SYSTEM_PROMPT
- Step 9–19: routes, tools, vector, indexer, watcher, web, bin, Dockerfile

## การตัดสินใจสำคัญ

- Vector disabled ก่อน — FTS only (ไม่ต้องตั้ง SOMA_EMBED_URL)
- ใช้ `bun install --ignore-scripts` เนื่องจาก esbuild postinstall conflict กับ vite

## ขั้นตอนถัดไป

```
xxx  →  Step 7: src/auth/tokens.ts + src/auth/middleware.ts
```
