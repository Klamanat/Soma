[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
target = "http-server"

[deploy]
startCommand = "bun bin/server.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 60

[[volumes]]
mountPath = "/app/data"
